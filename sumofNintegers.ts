/* 
整数NとN個の整数A1, A2, ⋯, ANが与えられます。（入力の形式は「入力」セクションを参照）

A1 + A2 +⋯+ A Nを出力してください。
*/
console.log("num = ?");
const sumOfnIntegers = () => {
  let lines: Array<string> = new Array();
  const readline = require("readline");
  const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout,
  });
  rl.on("line", () => {});
};
